# fasm compiling options
- `-m` option followed by a number, which specifies how many kb of memory
fasm should maximally use.
- `-p` option followed by a number can be used to specify the limit for
number of passes the assembler performs.
